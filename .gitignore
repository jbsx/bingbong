node_modules/
dist/
out/
graphify-out/
*.local
.DS_Store
*.tsbuildinfo
.env
# Latest real-model eval run (#109); the frozen comparison artifact is the
# committed e2e/eval/baseline.json, never this file.
e2e/eval/report.json

# Kiosk deployment runtime state + secrets (ADR 0023).
config/
data/

